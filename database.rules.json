{
  "rules": {
    ".read": "auth != null",
    ".write": "auth.token.email === 'admin@shifts-ta26.com'",
    "schedules": {
      "$week": {
        "pendingRequests": {
          "$reqId": {
            ".write": "auth != null && !data.exists()"
          }
        }
      }
    },
    "myRequests": {
      "$empId": {
        "$reqId": {
          ".write": "auth != null && !data.exists()"
        }
      }
    },
    "pendingRequestsIndex": {
      "$reqId": {
        ".write": "auth != null && !data.exists()"
      }
    },
    "forceDemands": {
      "$id": {
        ".write": "auth != null && !data.exists()"
      }
    },
    "passwordChangeRequests": {
      "$id": {
        ".write": "auth != null && !data.exists()"
      }
    },
    "taskSwapRequests": {
      "$id": {
        ".write": "auth != null && !data.exists()",
        "targetResponse": {
          ".write": "auth != null && !data.exists()"
        }
      }
    },
    "taskVolunteerRequests": {
      "$id": {
        ".write": "auth != null && !data.exists()"
      }
    },
    "holidaySignupRequests": {
      "$id": {
        ".write": "auth != null && !data.exists()"
      }
    },
    "holidaySwapRequests": {
      "$id": {
        ".write": "auth != null && !data.exists()",
        "targetResponse": {
          ".write": "auth != null && !data.exists()"
        }
      }
    },
    "weekendSwapRequests": {
      "$id": {
        ".write": "auth != null && !data.exists()",
        "targetResponse": {
          ".write": "auth != null && !data.exists()"
        }
      }
    }
  }
}
